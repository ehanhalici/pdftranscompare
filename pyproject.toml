[project]
name = "pdftranscompare"
version = "0.1.0"
description = "pdf viewer but translate and compare orianted"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "marker-pdf",
    "markdown",
    "BeautifulSoup4",
    "nltk",
    "line_profiler"
]
